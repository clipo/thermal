# Core dependencies for SGD Detection Toolkit
# IMPORTANT: scikit-learn 1.5.1 is required for model compatibility
numpy>=1.26.0,<2.0  # Updated for SAM compatibility
matplotlib>=3.3.0
Pillow>=8.0.0
scipy>=1.7.0
scikit-image>=0.18.0
scikit-learn==1.5.1  # EXACT version required for pre-trained model
opencv-python>=4.5.0,<4.10.0  # For image processing

# Optional for progress bars
tqdm>=4.60.0

# For polygon merging (creates unified SGD distribution maps)
shapely>=2.0.0

# For EXIF data extraction (GPS, timestamps, drone orientation)
piexif>=1.1.3
# Alternative: exifread>=3.0.0

# Optional for enhanced ML models (better segmentation accuracy)
# xgboost>=2.0.0
# lightgbm>=4.0.0

# ============================================================================
# OPTIONAL: SAM (Segment Anything Model) - For AI-based segmentation & detection
# ============================================================================
# SAM provides superior ocean/land segmentation and SGD detection accuracy
# Requires GPU (CUDA/MPS) for good performance
#
# To install SAM support:
#   bash scripts/setup_sam.sh
#
# Or manually:
#   pip install torch torchvision  # Install PyTorch for your platform first
#   pip install git+https://github.com/facebookresearch/segment-anything.git
#
# PyTorch installation (choose one based on your system):
# - CUDA (NVIDIA GPU): https://pytorch.org/get-started/locally/
# - Apple Silicon (M1/M2/M3): pip install torch torchvision
# - CPU only: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# SAM dependencies (installed by setup_sam.sh):
# torch>=2.0.0
# torchvision>=0.15.0
# segment-anything @ git+https://github.com/facebookresearch/segment-anything.git
# ============================================================================

# Note: For exact reproducibility across all systems, use:
# pip install -r requirements_exact.txt